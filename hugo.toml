baseURL = 'https://example.org/'
languageCode = 'en-us'
title = 'ReFi Barcelona'
theme = "smol"

[outputFormats]
  [outputFormats.JSON]
    mediaType = "application/json"
    isPlainText = true
    extension = "json"

[outputs]
  home = ["HTML", "RSS"]
  section = ["HTML", "RSS", "JSON"]
  page = ["HTML", "JSON"]
  taxonomy = ["HTML"]


[[menu.main]]
  identifier = "map"
  name = "Map"
  url = "/map/"
  weight = 10